# 实现与亮点

## 各模块实现情况

| 模块 | 负责人 | 实现状态 | 说明 |
|------|--------|----------|------|
| 订单模块 | 谢大伟 | ✅ 已完成 | 购物车生成订单、余额支付、商户签收、资金结算，`createAndPay` 事务一体化 |
| 退款/售后 | 谢大伟 | ✅ 已完成 | 退换货申请、商户审核（同意退款/换货/拒绝）、资金原路退回、30天有效期校验 |
| 商品模块 | 谢大伟 | ✅ 已完成 | CRUD、图片上传/读取/删除、全局搜索与商户内搜索、分页查询 |
| 支付宝充值 | 范吉明 | ✅ 已完成 | 沙箱 SDK 对接、扫码充值、异步通知回调、主动查询支付状态 |
| 购物车 | 范吉明 | ✅ 已完成 | Session 购物车增删改查、数据库持久化同步、登录自动加载 |
| WebSocket 聊天 | 范吉明 | ✅ 已完成 | 消费者与商户实时通讯、在线用户管理、消息持久化与历史查询 |
| 过滤器链 | 范吉明 | ✅ 已完成 | LoginFilter 白名单放行 + RoleFilter 角色路径拦截，401/403 统一响应 |
| 认证模块 | 邱凯鹏 | ✅ 已完成 | 消费者/商户/管理员注册登录、Session 管理、游客模式、登出 |
| 消费者管理 | 邱凯鹏 | ✅ 已完成 | 个人信息查询修改、删除校验 |
| 商户管理 | 邱凯鹏 | ✅ 已完成 | 店铺信息管理、店铺名修改 |
| 评价模块 | 邱凯鹏 | ✅ 已完成 | 1-5分评价提交、签收后3月内可评、评价详情查看 |
| 管理员后台 | 李嘉杰 | ✅ 已完成 | 用户/订单查看、今日营收统计、启用/禁用用户 |
| 定时任务 | 李嘉杰 | ✅ 已完成 | 每10分钟清理超时未付订单、每天16点清理过期订单 |
| 基础配置 | 李嘉杰 | ✅ 已完成 | MyBatis-Plus 自动填充、支付宝参数绑定、跨域与静态资源配置 |

---

## 遇到的问题与解决办法

### 1. 项目重命名导致启动失败

**现象**：项目从 `MysqlConnected` 重命名为 `TBServer` 后，IDEA 运行配置中的主类名未同步更新，报错 `ClassNotFoundException: com.taobao.MysqlConnectedApplication`。

**解决**：修改 `.idea/workspace.xml` 中的 `SPRING_BOOT_MAIN_CLASS` 为正确的 `com.taobao.TBServerApplication`，同步更新 `compiler.xml` 中的模块名。

### 2. 全局 CSS 与 Tailwind 样式冲突

**现象**：`style.css` 中 `input[type="text"]` 的 `padding-left: 14px` 属性选择器优先级高于 Tailwind 的 `pl-10` 类选择器，导致输入框左侧图标与 placeholder 文字重叠。

**解决**：在受影响的输入框上改用内联样式 `style="padding-left: 40px"` 绕过优先级问题。

### 3. 注册页面布局失调

**现象**：注册卡片过宽（`max-w-2xl`），与登录页风格不统一，表单元素间距不合理。

**解决**：改为 `max-w-md` 与登录页一致，新增表单标题图标区，统一视觉风格。

### 4. Session 购物车与数据库同步

**现象**：游客模式下购物车存 Session，登录后需将数据库中的购物车数据合并到 Session，退出时需持久化回数据库。

**解决**：登录时调用 `loadCart` 从数据库加载到 Session，退出/保存时调用 `saveCart` 覆盖写入数据库，保证两端数据一致。

### 5. 支付宝异步通知验签

**现象**：支付宝沙箱异步通知需要验签确保请求真实性，且本地开发环境无法被外网回调。

**解决**：使用支付宝 SDK 提供的 RSA 验签方法校验通知签名；开发阶段通过内网穿透工具暴露回调地址，配合主动查询接口兜底。

### 6. 跨模块资金流转一致性

**现象**：支付时扣消费者余额 → 订单状态改为已支付 → 签收时资金从暂存转入商户营收，涉及多表更新需保证原子性。

**解决**：在 `OrdersServiceImpl` 中通过 Spring 事务注解 `@Transactional` 包裹支付和签收操作，异常时回滚保证数据一致。

---

## 亮点功能展示

### 1. 过滤器链双重鉴权

`LoginFilter` → `RoleFilter` 链式拦截，白名单路径免登录（静态资源、商品浏览等），其余请求先验登录再验角色，未登录返回 401、无权限返回 403，统一 JSON 格式响应。角色路径采用前缀匹配，消费者/商户/管理员各自只能访问专属接口。

### 2. WebSocket 实时聊天

基于 Spring WebSocket 实现消费者与商户即时通讯，`ConcurrentHashMap` 维护在线用户映射（key=`role_id`），消息实时转发给在线用户并持久化到数据库，离线用户下次登录可查历史记录。支持消费者主动发起新会话。

### 3. 支付宝沙箱充值

对接支付宝开放平台沙箱环境，用户在前端输入充值金额后调用后端生成付款二维码，支持扫码支付。异步通知回调处理充值到账，同时提供主动查询接口作为兜底，避免通知丢失。

### 4. 订单生命周期管理

订单状态从"购物车生成未支付"→"已支付待发货"→"已签收"完整流转。`createAndPay` 接口一次调用完成购物车选品→生成订单→余额扣款→清空购物车，中间任何一步失败均回滚。定时任务自动清理超时未支付订单。

### 5. 退换货审核流程

消费者在签收后 30 天内可申请退换货，系统校验"已签收、未超期、未重复申请、未评价"。商户审核可选择同意退款（资金原路退回）、同意换货或拒绝。售后状态实时可查。

### 6. 定时任务自动化清理

`@Scheduled` 实现两个定时任务：每10分钟清理超时未支付订单释放库存，每天下午4点清理签收超过3个月的历史订单，保持数据库轻量。

### 7. 游客模式

未登录用户可浏览商品、搜索商品、查看商品图片，点击"游客登录"无需账号即可进入商城，降低使用门槛。

### 8. 购物车双端同步

Session 购物车支持游客即时操作，登录时自动从数据库加载持久化购物车数据合并到 Session，保存/退出时覆盖写回数据库，保证登录前后购物车数据不丢失。
