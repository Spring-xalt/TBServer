# TBServer 仿淘宝电商平台 — 项目分工与架构

## 项目架构

Spring Boot 3 + MyBatis-Plus + MySQL 单体电商后端，含支付宝沙箱支付、WebSocket 即时通讯、定时任务。MVC 分层：Controller → Service → Mapper → Entity，Filter 链做登录鉴权 + 角色权限控制。前端为 Tailwind CSS + Axios 单页。

**技术栈**：Java 17 / Spring Boot 3.5.5 / MyBatis-Plus / MySQL / WebSocket / 支付宝沙箱 / Maven

---

## 人员分工（按业务模块）

### 范吉明 — 支付 + 购物车 + 即时通讯 + 安全

| 子模块 | 说明 |
|--------|------|
| 支付宝充值 | 支付宝沙箱 SDK 对接，生成付款二维码，异步通知验签与回调处理，主动查询支付状态，充值到账 |
| 购物车模块 | Session 购物车（增删改查 + 清空）、数据库持久化同步、登录时加载购物车数据 |
| WebSocket 聊天 | 消费者与商户实时通讯、在线用户管理（ConcurrentHashMap）、消息持久化与历史记录查询 |
| 过滤器链 | LoginFilter（登录校验、白名单放行）+ RoleFilter（角色权限、路径拦截），未登录 401 / 无权限 403 |

### 谢大伟 — 商品 + 订单 + 退款售后

| 子模块 | 说明 |
|--------|------|
| 商品模块 | 商品 CRUD、图片上传/读取/删除（文件系统存储）、全局搜索与商户内搜索、分页查询 |
| 订单模块 | 购物车→订单→支付→签收→资金结算完整链路，多表事务，订单状态机 |
| 退款/售后模块 | 退换货申请与审核（同意退款/换货/拒绝），资金原路退回，30天有效期、不可重复申请等业务约束 |

### 邱凯鹏 — 用户体系 + 评价模块

| 子模块 | 说明 |
|--------|------|
| 认证模块 | 消费者/商户/管理员注册登录、Session 管理、游客模式、登出 |
| 消费者管理 | 个人信息查询与修改、删除（校验订单状态） |
| 商户管理 | 店铺信息管理、店铺名修改、管理员更新商户信息 |
| 评价模块 | 消费者提交评价（1-5分 + 文字）、签收后3个月内可评、评价详情、商户查看店铺评价 |

### 李嘉杰 — 后台管理 + 定时任务 + 基础配置

| 子模块 | 说明 |
|--------|------|
| 管理员后台 | 查看所有消费者/商户/订单、今日签收营收统计、启用/禁用用户 |
| 定时任务 | 每10分钟清理超时未支付订单、每天16点清理已签收超3个月的订单 |
| 基础配置 | MyBatis-Plus 自动填充时间戳、支付宝配置绑定、全局跨域与静态资源配置 |

---

## 模块关系简图

```
[过滤器链: LoginFilter → RoleFilter] ← 范吉明
              │
   ┌──────────┼──────────┬──────────────┐
   ▼          ▼          ▼              ▼
 认证(邱)   商品(谢)   订单(谢)       后台(李)
   │          │          ├ 创建+支付     ├ 用户管理
   ▼          ▼          ├ 签收结算      ├ 营收统计
 消费者     购物车(范)   ├ 退款(谢)      └ 启/禁用
 +商户      WebSocket   └ 支付宝(范)
 +评价(邱)  (范)
```

> **注意**：分工按业务模块划分，不按前后端分层。每个模块负责人同时负责该模块的 Controller、Service、Mapper、前端页面的开发与联调。
